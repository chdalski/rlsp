[package]
name = "rlsp-yaml"
version = "0.1.0"
edition = "2024"

[lints]
workspace = true

[dependencies]
tower-lsp = { version = "0.20", features = ["proposed"] }
tokio = { version = "1", features = ["full"] }
serde_json = "1"
yaml-rust2 = "0.9"
regex = "1"
once_cell = "1"

[dev-dependencies]
futures = "0.3"
tower = "0.4"
