---
name: Follow-up task queue
description: Remaining items after parser implementation, conformance hardening, migration, and workaround removal
type: project
---

<!-- Only track open items here. Completed work lives in its plan file
     and git history — duplicating it here adds noise and stale state.
     Remove items when their plan is marked Completed. -->

## Open: rlsp-fmt

## Open: rlsp-yaml

<!-- helper-of: convention — an allow-list entry marked `HelperOf` exists because its root
     feature function is also allow-listed as a `TodoRetrofit`. When the root's retrofit plan
     lands, all `HelperOf` entries pointing at that root are removed from the allow-list at the
     same time; they are NOT independent retrofit items and do NOT need their own follow-up
     plans. -->

<!-- Audit-v2 feature-level retrofits — 12 public feature entry points that hand-roll YAML
     scanning instead of consuming the parser AST. Each item below has its signature, violation
     shape, replacement sketch, and the private helpers retired when the root retrofit lands.
     These were surfaced and allow-listed in commit c70f642 under
     .ai/plans/2026-04-18-parser-boundary-audit-v2.md Task 1. -->



- **`block_to_flow` policy enforcement against `formatEnforceBlockStyle`** — When the user sets `formatEnforceBlockStyle: true`, the workspace formatter rewrites all flow-style collections back to block style on save. The `block_to_flow` code action contradicts this policy: applying it produces flow output that the next save reverts. Open question: should `block_to_flow` be suppressed (not offered) when `formatEnforceBlockStyle: true`, or should it remain offered with a UI hint, or is the conflict acceptable as user-driven? Surfaced during the code-action user-format-config plan audit (`.ai/plans/2026-04-27-code-action-respect-user-format-config.md`, in Decisions/Non-Goals). Separate plan needed — the question is policy/UX, not a correctness bug. Related but distinct: are there other code actions whose existence violates user formatter policy when settings are non-default?

- **`autoWrapFlowStyle` opt-out for `block_to_flow`** — The code-action user-format-config plan (`.ai/plans/2026-04-27-code-action-respect-user-format-config.md`) sets `block_to_flow`'s default to auto-wrap output to fit `formatPrintWidth`, removing the misleading `(long line)` title suffix. If users later report wanting single-line flow output regardless of length (e.g., for tooling that prefers compact JSON-like flow), add a setting like `autoWrapFlowStyle: false` (default `true`) so the action emits unwrapped flow when the user opts out. Defer until evidence shows demand — pre-emptively shipping the setting risks configuration sprawl across all code actions. Bias: do nothing unless a user explicitly asks for it.

- **Document rlsp-yaml ↔ prettier (and other formatters) interop** — The code-action user-format-config plan (`.ai/plans/2026-04-27-code-action-respect-user-format-config.md`) made code-action output use rlsp-yaml's `formatPrintWidth`. Many users run rlsp-yaml as their LSP server but prettier (or a different formatter) as their format-on-save formatter. In that setup the user maintains two parallel print-width settings: rlsp-yaml's `formatPrintWidth` (controls code-action output shape) and prettier's `printWidth` (controls save-time reformatting). Both default to 80, so most users won't notice — but a customized one without the other produces mid-edit visual jitter (code action wraps for 80; save reformats to 100 or vice versa). Documentation-only follow-up: add a section to `rlsp-yaml/docs/configuration.md` explaining the interaction, listing which rlsp-yaml settings have prettier equivalents (`formatPrintWidth`/`printWidth`, `formatSingleQuote`/`singleQuote`, `formatBracketSpacing`/`bracketSpacing`), and recommending users keep them aligned when both formatters are active. **Out of scope:** automatic cross-formatter config awareness (e.g., reading `.prettierrc` for fallback values) — that's a much heavier change and invites a "which other prettier knobs do we honor?" rabbit hole. Start with documentation; consider richer interop only if user demand emerges.

- **Custom tag type annotations** — RedHat's customTags supports `!include scalar`, `!ref mapping` type annotations. Ours is a plain string allowlist — add type annotation support.
- **Non-printable unicode character diagnostic (LSP layer only)** — The parser now enforces c-printable on all literal stream content and nb-json on quoted scalars (commit: fix(rlsp-yaml-parser): reject non-c-printable characters in literal stream input). Non-printable characters produce structured `Error` events from `parse_events()`. Remaining LSP work: surface those parse errors as LSP diagnostics with diagnostic code `invalidCharacter` and the position information already carried in the `Error` struct. No parser changes needed — the parser does the enforcement; the LSP layer translates `Error` results to LSP `Diagnostic` objects.
- **Expand `block_to_flow` code action to support nested block structures** — The action currently refuses nested inputs via `return None` in `code_actions.rs:420`. The `2026-04-18-retrofit-block-to-flow-code-action.md` plan preserved this narrow behavior to keep scope minimal (bug-class elimination, not feature expansion). After the retrofit lands, the AST+formatter path handles nesting automatically — lifting the restriction is cheap. Enhancement plan: remove the pre-check, add tests for nested block-to-flow conversions, confirm the formatter produces correct flow output (e.g., `{a: {b: 1}}`, `[[1, 2], [3, 4]]`).

- **Offer folded block scalar (`>`) as an alternative output form for the `string_to_block_scalar` code action** — The `2026-04-18-retrofit-string-to-block-scalar-code-action.md` plan converts strings to `ScalarStyle::Literal` (`|`) only, preserving the current behavior. Literal preserves newlines verbatim; folded collapses line breaks into spaces (better for prose). After the retrofit lands, offering `>` as a SECOND quick-fix alongside "Convert to block scalar (literal)" is a small UI enhancement: add a `Node::Scalar` clone path targeting `ScalarStyle::Folded`, emit a separate `CodeAction` titled "Convert to folded block scalar". User picks between literal and folded at apply time.
- **Expand `string_to_block_scalar` code action to sequence-item scalars** — The `2026-04-18-retrofit-string-to-block-scalar-code-action.md` plan preserved the current mapping-values-only dispatch (AST walk only looks at `Node::Mapping.entries` values, skipping `Node::Sequence.items`). Symmetric with the `block_to_flow` nested-support enhancement above. A long string like `- "this is a long sequence-item string"` could benefit from block-scalar form too. Enhancement plan: extend the AST walk to also match qualifying `Node::Scalar` values inside `Node::Sequence.items`, add regression tests for sequence-item conversion, verify the formatter produces correct `- |\n  content` output.
- **Corpus invariant: validator stability under whitespace re-emit** — Originally deferred in Move 0 (`.ai/plans/2026-04-18-corpus-invariants-scaffold.md`) under the name "I5," but slot I5 has since been reassigned to the AST anchor_loc invariant in `tests/corpus_invariants.rs`. The next free slot is I11 (I10 was taken by the formatter round-trip invariant in plan `2026-04-28-corpus-invariant-formatter-round-trip.md`). For each corpus file and each validator, run the validator on the original text AND on a whitespace-only re-emit of the same document; assert the set of diagnostic codes is identical (ranges may shift). Catches validators whose output depends on whitespace quirks rather than structure.
- **Expand corpus beyond the 4 seed files** — Move 0 seeded the corpus with `release-plz-workflow.yml`, `kubernetes-deployment.yaml`, `docker-compose.yml`, `github-actions-matrix.yml`. Real-world YAML covers many more shapes: Ansible playbooks, Helm chart templates, GitLab CI pipelines, CloudFormation/CDK YAML, Prometheus alert rules, SOPS-encrypted files, Swagger/OpenAPI specs, Argo CD `Application` manifests, Flux CD `Kustomization`s, Tekton `Pipeline`/`Task` resources. Each adds new coverage. File as one plan per shape, or a batch-add plan for 3-5 at a time. Each new file may surface new I4 failures that flag latent bugs — treat those under the Surprise Failure Protocol.
## Open: rlsp-yaml-parser

- **Post-Phase-2 conformance workflow (master orchestration)** — After the Phase 2 normative-prose audit (`.ai/plans/2026-04-30-yaml-spec-conformance-audit-phase2.md`, currently drafted but uncommitted) executes, drive the remaining conformance work through these steps in order. Each step gates the next.
  1. **Reconcile Phase 2 findings.** Apply the same dual-track methodology, summary, and follow-up filing as Phase 1. If Phase 2 surfaces `[NEEDS USER REVIEW]` items, the lead investigates per the established process and documents the resolution durably (mirroring Phase 1's §7.3.x-vs-§7.4.2 BNF-trace at `.ai/audit/2026-04-30-phase1-bnf/reconciliation-§7.md` entry [110]). Phase 2 may also surface additional Lenient findings that need user decisions in step 2.
  2. **Bundle conformance design decisions (Lenient findings).** Combine Phase 1's 11 Lenient entries with Phase 2's findings. For each Lenient entry, capture the user's choice as either (a) FIX — becomes its own implementation plan in step 4, or (b) FORMALLY ACCEPT — with a written rationale that propagates into the conformance doc rewrite in step 5. Without explicit user choices, the doc rewrite cannot be authoritative.
  3. **Stricter-than-spec rationale review.** Re-examine each Stricter-than-spec entry to confirm rationale is durable. Phase 1 found 5: [59]/[60]/[61] are well-rationalized as security hardening (Trojan Source mitigation, escape obfuscation rejection); [86] (major-0 rejection) and [87] (u8 digit limit) have thinner pragmatic rationales — user decides whether to keep, strengthen the rationale, or relax to spec-strict. Phase 2 may add more.
  4. **Implementation plans for FIX decisions.** Each Lenient finding the user chose to fix becomes its own plan (e.g., `2026-XX-XX-fix-c-printable-enforcement.md`). Each plan addresses the gap at the production where the rule is enforced; cross-reference any propagated leniency in the audit summary to bound scope correctly. Several Phase 1 Lenient findings share roots ([1]/[27]/[34]/[75] all stem from c-printable enforcement; [93]/[94]/[95] share tag-prefix-validation root) and may be fixed in one plan each, not separately.
  5. **Conformance doc rewrite plan.** Once all decisions are made and FIX plans land, draft a plan to rewrite `rlsp-yaml-parser/docs/yaml-spec-conformance.md`. Required content for the rewrite:
     - Embed the BNF-trace analyses captured in audit reconciliation files verbatim — especially the §7.3.x-vs-§7.4.2 prose-vs-BNF terminology analysis at `reconciliation-§7.md`'s [110] entry. Future readers must be able to "grasp the understanding at a glance."
     - Document the verdict taxonomy in use: Strict-conformant, Stricter-than-spec, Lenient (with intentional-acceptance rationale), Non-conformant, Not-applicable, Indeterminate.
     - For every Strict-conformant entry, cite spec quote + BNF + implementation file:line + at least one example or test reference.
     - For every Stricter-than-spec entry, document the security/pragmatic rationale and the spec deviation precisely.
     - For every "formally accepted" Lenient entry, document the spec gap, the implementation choice, and why the gap is accepted.
     - Adopt the symmetric reconciliation principle ("attribute strictness/leniency to the production where the rule is enforced; parent productions that correctly compose are conformant") as the documented audit methodology so future audits stay consistent.
     - Replace the misleading "Conformant" labels on the 11+ entries the audit found mislabeled (the Doc Errata listing in `summary.md`).
     - Fix citation drift at [29] (cites unused `normalize_line_breaks`) and [105] (off by ~5 lines).
  6. **Conformance status declaration.** Once the doc reflects audited reality, the project can publicly state its conformance posture (e.g., "rlsp-yaml-parser is YAML 1.2.2 conformant per §3-§9 BNF productions and the Phase 2 normative-prose requirements; documented exceptions are listed under Stricter-than-spec or Formally-Accepted-Lenient").

  **Pickup notes (so a future session resumes correctly):**
  - All 11 Phase 1 Lenient entries are now resolved: [1]/[27]/[34]/[75] by the c-printable enforcement fix (`fix(rlsp-yaml-parser): reject non-c-printable characters in literal stream input`); [84]/[85] by directive ns-char validation fix; [93]/[94]/[95]/[99] by tag prefix and shorthand suffix validation fix; [69] resolved by flow-line-prefix indent enforcement (`fix(rlsp-yaml-parser): enforce s-indent(n) on quoted scalar continuation lines`). All Phase 1 Lenient entries resolved — step 2 can proceed without individually-filed queue entries.
  - The 9 entries that needed BNF-trace analysis to resolve verdict ([110]/[121]/[131] in Phase 1) had their analysis captured in the reconciliation file and propagated to summary; future audits should preserve this pattern — when the lead resolves a `[NEEDS USER REVIEW]` item, the resolution rationale lives in the audit record so it doesn't need to be re-researched.
  - The `[83] ns-reserved-directive` resolution (Strict-conformant via "should is non-mandatory" reading) is correct but with relatively thin rationale — worth a brief BNF/prose re-check during step 5's doc rewrite to harden the doc entry.

- **Double BOM at stream start silently accepted (§5.2)** — Two BOM-strip code paths overlap for the first document: `rlsp-yaml-parser/src/lines.rs:115-127` (`scan_line` `is_first=true`) AND `lines.rs:282-305` (`signal_document_boundary` for the first document's BetweenDocs → InDocument transition). Inter-document transitions correctly run only the second path and reject second consecutive BOM. Fix: gate the second strip on "no BOM was already stripped at this prefix," or remove redundancy at one of the two sites. Doc citation at `yaml-spec-conformance.md:149-151` overstates uniformity (cites a test that only exercises the inter-doc case). Surfaced by `2026-04-30-phase2-prose/reconciliation-§5.2.md` (defect L1).
- **`MAX_DIRECTIVES_PER_DOC=64` hardcoded limit (§6.8) — Stricter-than-spec, awaiting user decision** — `event_iter/directives.rs:75-83` hard-rejects on the 65th directive in a single document. Spec has no per-document directive count limit; this is defensive but undocumented as a configurable choice. User decides post-Phase-2: keep limit (current), make configurable via `LoaderOptions`, or remove. Surfaced by `2026-04-30-phase2-prose/reconciliation-§6.8.md` (REQ-16). Stricter-than-spec entries do not auto-file as fix-tracking; this entry is the design-decision tracker.
- **Core leading-zero decimal rejection (§10.3) — Stricter-than-spec, awaiting user decision** — `schema.rs:307-309` rejects `007`, `01`, `0123` as `!!str` though spec regex `[-+]? [0-9]+` permits leading zeros. Defensive (prevents user-error confusion with octal — many YAML producers wrote `007` intending octal under YAML 1.1 conventions). User decides post-Phase-2: keep defensive rejection (current), document the strictness rationale, or relax to spec-permissive. Surfaced by `2026-04-30-phase2-prose/reconciliation-§10.3.md` (REQ-7). Stricter-than-spec; design-decision tracker.
- **Audit testing-methodology coverage gaps across the codebase** — Phase 2 §5.2 audit revealed that the BOM-less UTF-32 detection gap was missed because the existing test corpus had no BOM-less UTF-32 fixtures and no property-based test asserted "encoding-invariant parse output." This is a meta-pattern: spec tables transcribed incompletely into the implementation, normative invariants not expressed as proptest properties. Survey the `rlsp-yaml-parser`, `rlsp-yaml`, and `rlsp-fmt` codebases for similar testing-methodology gaps. Specifically:
  - **Spec-table coverage:** identify every normative spec table the implementation dispatches on (e.g., §5.2 encoding detection, §10.2/§10.3 integer/float regex tables, §6.9.1 tag-resolution rules, §10.1 default-tag-by-kind). For each table, verify every row is exercised by at least one positive test fixture. Surface rows with no test as gap entries.
  - **Behavioral invariants as proptest properties:** identify invariants that could be expressed as `proptest!` properties (e.g., "encoding choice is invariant under parse," "round-trip `parser → formatter → parser` preserves AST," "every codepoint accepted by lexer L1 is consistent with predicate P1"). Surface invariants that exist informally in code/comments but lack a property test.
  - **Differential testing opportunities:** identify behaviors where comparing against a reference implementation (libyaml / PyYAML / snakeyaml) would surface incomplete-dispatch bugs. Surface high-value targets (e.g., the full §5.2 table; schema resolution under each of Failsafe/JSON/Core).
  - **Output:** a survey document at `.ai/audit/<date>-testing-methodology-gaps/summary.md` with one entry per gap. Each entry: gap description, what bug class it would catch, specific fix recommendation (fixture / property / differential case). Similar in shape to a conformance audit reconciliation file.
  - **Builds on:** the encoding round-trip proptest infrastructure being added in the BOM-less UTF-32 fix plan — once that property exists, expanding to other invariant families is incremental.
  - **Suggested timing:** after the BOM-less UTF-32 fix lands (so the round-trip property pattern exists as a model). Could be folded into the post-Phase-2 design-decisions batch if user wants the testing-methodology decision-makings together with the conformance-fix decisions.

- **Error-position imprecision across 6 error classes (error-and-limits)** — Errors are correctly produced and structured (no panics, typed `Error`/`LoadError`), but `pos` field doesn't point to the offending byte for: (1) `%YAML` major-0 rejection (reports `%`); (2) `%YAML` u8 digit overflow (reports `%`); (3) unterminated single-quoted scalar (reports EOF); (4) resolved-tag overflow (reports `---` line); (5) `MAX_ANCHOR_NAME_BYTES` overflow (reports `&` start-of-anchor); (6) `LoadError::UndefinedAlias` / `CircularAlias` / `AnchorCountLimitExceeded` / `AliasExpansionLimitExceeded` / `NestingDepthLimitExceeded` carry no `pos` field at all. Fix shape uniform: capture byte position at precise overflow/error point and pass through to error construction (the implicit-key 1024 limit at `event_iter/flow.rs:1136-1161` and `event_iter/block/mapping.rs:164` demonstrates the precise-byte design that other paths could mirror). Spec is silent on positions; this is a usability defect against the implementation contract. Surfaced by `2026-04-30-phase2-prose/reconciliation-error-and-limits.md` (defects L12-L17, consolidated single fix entry).

- **Conformance doc rewrite to reflect audit findings** — `rlsp-yaml-parser/docs/yaml-spec-conformance.md` requires substantive rewrite to reflect Phase 1 + Phase 2 audit findings:
  - **Phase 1 mislabels** (Phase 1 audit, all confirmed `Lenient` behaviorally where applicable): [1], [27], [34], [75]. ([93], [94], [95], [99] resolved by tag prefix and shorthand suffix validation fix; [69] resolved by flow-line-prefix indent enforcement fix.)
  - **Phase 2 doc errata:** §5.2 BOM rejection citation overstates uniformity (cites a test that only covers the inter-doc case, not stream-start); §6.8 directive-name/parameter validation labels contradicted by behavioral evidence; §10.3 integer rows lack documentation of leading-zero rejection or signed-octal/hex laxity; position-precision contract not documented (need per-error-class table); `%YAML 1.0` accepted refinement (only major=0 is rejected, not minor=0); 1 MiB quoted-scalar cap actual coverage (escapes-only).
  - **Spec errata to surface:** §10.2 worked example for `-0` (line 6601) contradicts the int regex (line 6578); the parser correctly follows the regex (`-0` → `!!float`). Document the parser's literal-regex choice and the spec's internal inconsistency.
  - **Citation errata:** [29] (cites unused function `normalize_line_breaks`); [105] (citation off by ~5 lines).
  - **Architectural findings to document** (from Phase 2): no Warning event variant or collector (affects multiple "should warn" cases); loader bare-`!` shortcut conflates verbatim and shorthand sources (prevented from reaching loader now that admissibility rejects `!<!>`); position-precision design contract is implicit (most errors point to start-of-construct rather than offending-byte).
  - **BNF-trace analyses to embed verbatim:** §7.3.x-vs-§7.4.2 prose-vs-BNF terminology trap (Phase 1 `reconciliation-§7.md` entry [110]); the Phase 1 `[NEEDS USER REVIEW]` items ([110]/[121]/[131]) were resolved 2026-04-30 as `Strict-conformant`.
  - The doc rewrite is gated on user design decisions on Lenient findings (see post-Phase-2 orchestration entry's step 5). Plan content requirements are listed there.
- create bindings for python - https://pyo3.rs and typescript (wasm)
